# E-DAIC 新增标注样本的探索性补充验证

> 模块 14 · E-DAIC 扩展验证 · 分析日期 2026-07-11
> 复现：仓库 `codex/reanalysis-v2` 分支；从原始 E-DAIC 重建 C5 证据需本地授权数据与 `APIYI_API_KEY`。含原文的抽取中间文件不上传 GitHub。

## 0 研究定位（硬性声明）

1. **E-DAIC 是 DAIC-WOZ 的扩展版本，不是完全独立的外部数据库。** 原始 DAIC-WOZ 的 189 名参与者已被完整并入 E-DAIC；E-DAIC 另新增 86 人，其中仅 30 人有公开 PHQ-8 标签。
2. 本模块**只纳入 E-DAIC 新增且公开 PHQ-8 标签**的参与者（来自 `Detailed_PHQ8_Labels_E_DAIC_only.csv`），排除原始 DAIC-WOZ 189 人与无公开标签的 test 56 人。
3. **不与 DAIC-WOZ 主分析样本合并**；E-DAIC 仅作为新增有标签测试集。
4. E-DAIC 结果**只作探索性补充验证**，用于观察 DAIC-WOZ 主分析发现的现象方向是否可复现。
5. 若方向一致，表述为「方向上支持」；**不写为「严格外部验证」**。（本次新增样本与 DAIC-WOZ 训练样本 ID 无重叠，但 E-DAIC 数据资源继承 DAIC-WOZ 的采集框架，且样本量小。）
6. 若方向不一致，诚实说明「E-DAIC 新增小样本中未能稳定复现」。
7. **PHQ-8 为自评量表总分，不是临床诊断**；所有结论描述「自评抑郁症状严重度/风险」，不能外推为临床诊断。
8. 本模块的**公开产物不包含任何访谈原文**，仅发布 participant_id 与结构化指标；含原文的本地抽取输入、span 和 checkpoint 均排除在版本控制之外。

## 1 方法（可粘贴进论文 2.x）

为检验主分析发现的可迁移性，本研究进一步使用 E-DAIC 中新增且公开 PHQ-8 标签的参与者作为探索性补充验证样本。由于 E-DAIC 为 DAIC-WOZ 的扩展版本并包含原始 DAIC-WOZ 参与者，本研究未将两者合并，而是排除原始 DAIC-WOZ 参与者、仅保留新增有标签样本（候选 30 人，纳入 n=22，排除 8 人：transcript_empty）。该补充分析沿用 DAIC-WOZ 主分析中的十域症状定义、C5 抽取 prompt（v3）、模型参数（gpt-5.5 / temperature=0）与聚合规则生成症状证据特征。为规避接口对长输入的空响应，全文按不超过 3500 字符、相邻重叠 200 字符分块抽取，合并时按参与者、症状域、极性和原句做语义去重。由于 E-DAIC 转录本缺少 speaker 标记，抽取输入为无 speaker 标记的全对话文本，因此该补充验证并非完全同源输入条件下的严格复现。模型在 DAIC-WOZ 主分析样本（n=142）上训练，并在 E-DAIC 新增样本上直接测试。

> **偏差披露**：E-DAIC 转录本无 speaker 列（DAIC-WOZ 原始转录本含 Ellie/Participant 标签），故对 E-DAIC 喂入全对话文本；C5 抽取 prompt 要求仅提取被试症状证据，但由于 E-DAIC 输入为无 speaker 标记的全对话文本，仍可能存在访谈员话语干扰，因此该补充验证仅作探索性分析。

## 2 结果（可粘贴进论文 3.4）

E-DAIC 新增有标签样本共纳入 n=22，其中 PHQ-8 阳性 n=6。采用 DAIC-WOZ 固定切点定义访谈证据高低（覆盖广度 ≥5 个症状域 / 证据密度 ≥11 条）。
- 覆盖广度定义下，错位样本占 9/22（40.9%）；证据密度定义下占 9/22（40.9%）。
- DAIC-WOZ 训练出的基础负荷模型迁移到 E-DAIC 后，错位样本错分率为 1.000、一致样本为 0.000；
  计数模型为 0.667 / 0.231，出现模型为 0.889 / 0.077（覆盖广度预定义切点）。
  该方向与 DAIC-WOZ 主分析一致（错位样本错分率高于一致样本）。

## 3 讨论（可粘贴进论文讨论）

E-DAIC 新增标注样本的探索性结果在方向上支持 DAIC-WOZ 主分析：自评—访谈证据错位现象可观察到，且以访谈证据为输入的模型其错误向错位样本集中。但鉴于新增公开标签样本量有限（n=22），且 E-DAIC 继承 DAIC-WOZ 的采集框架，本研究不将其解释为严格外部验证；该结果应视为对主分析结论的探索性补充。

## 4 对照表（摘要）

| 指标 | DAIC-WOZ 主分析 | E-DAIC 新增样本 | 方向一致 |
|------|-----------:|----------:|------|
| 样本量 | 142.0 | 22.0 | — |
| PHQ 阳性比例 | 0.303 | 0.273 | — |
| 覆盖广度错位比例(%) | 31.7 | 40.9 | 是 |
| 证据密度错位比例(%) | 19.0 | 40.9 | 是 |
| base 模型 AUC | 0.781 | 0.688 | 是 |
| count 模型 AUC | 0.8 | 0.583 | 是 |
| pres 模型 AUC | 0.796 | 0.583 | 是 |
| base 模型错位错分率(覆盖广度) | 0.867 | 1.0 | 是 |
| base 模型一致错分率(覆盖广度) | 0.041 | 0.0 | — |
| count 模型错位错分率(覆盖广度) | 0.511 | 0.667 | 是 |
| count 模型一致错分率(覆盖广度) | 0.083 | 0.231 | — |
| pres 模型错位错分率(覆盖广度) | 0.578 | 0.889 | 是 |
| pres 模型一致错分率(覆盖广度) | 0.124 | 0.077 | — |
| base 模型错位错分率(证据密度) | 0.741 | 0.889 | 是 |
| base 模型一致错分率(证据密度) | 0.2 | 0.077 | — |
| count 模型错位错分率(证据密度) | 0.63 | 0.667 | 是 |
| count 模型一致错分率(证据密度) | 0.122 | 0.231 | — |
| pres 模型错位错分率(证据密度) | 0.667 | 0.889 | 是 |
| pres 模型一致错分率(证据密度) | 0.174 | 0.077 | — |

## 5 输出文件

- `edaic_extension_sample_manifest.csv`：30 候选→纳入清单与排除原因。
- `edaic_evidence_features.csv`：10 域 count/presence + coverage/evidence。
- `edaic_mismatch_quadrants_by_coverage.csv` / `_density.csv`：四象限。
- `edaic_transfer_predictions.csv` / `edaic_transfer_metrics_all_models.csv`：迁移预测与指标。
- `edaic_model_error_by_mismatch_group.csv`：错位 vs 一致错分率 + Fisher。
- `daic_vs_edaic_validation_comparison.csv`：主分析 vs 扩展验证对照。
- `run_manifest.json` / `output_manifest_sha256.csv`：参数与可复现性清单。
