# E-DAIC 新增标注样本的描述性补充分析

> 模块 14 · E-DAIC 描述性扩展 · 分析日期 2026-07-11
> 复现：仓库 `codex/reanalysis-v2` 分支；从原始 E-DAIC 重建 C5 证据需本地授权数据与 `APIYI_API_KEY`。含原文的抽取中间文件不上传 GitHub。

## 0 研究定位（硬性声明）

1. **E-DAIC 是 DAIC-WOZ 的扩展版本，不是完全独立的外部数据库。** 原始 DAIC-WOZ 的 189 名参与者已被完整并入 E-DAIC；E-DAIC 另新增 86 人，其中仅 30 人有公开 PHQ-8 标签。
2. 本模块**只纳入 E-DAIC 新增且公开 PHQ-8 标签**的参与者（来自 `Detailed_PHQ8_Labels_E_DAIC_only.csv`），排除原始 DAIC-WOZ 189 人与无公开标签的 test 56 人。
3. **不与 DAIC-WOZ 主分析样本合并**；E-DAIC 仅作为新增有标签测试集。
4. E-DAIC 结果仅用于**固定错位定义的描述性可迁移性检查**，以及迁移模型性能点估计的描述性报告。
5. **错位分组与迁移模型存在定义—特征重叠。** 分组使用 coverage_breadth / evidence_density，三个迁移模型均直接使用这两项变量，count / presence 模型还使用其域级组成，因此错位组错分率不能视为独立的误差机制验证。
6. 迁移性能须同时报告 95% CI 与样本量；区间跨过 0.5 时，不把 AUC 点估计解释为泛化性能证据。
7. **PHQ-8 为自评量表总分，不是临床诊断**；所有结论描述「自评抑郁症状严重度/风险」，不能外推为临床诊断。
8. 本模块的**公开产物不包含任何访谈原文**，仅发布 participant_id 与结构化指标；含原文的本地抽取输入、span 和 checkpoint 均排除在版本控制之外。

## 1 方法（可粘贴进论文 2.x）

本研究使用 E-DAIC 中新增且公开 PHQ-8 标签的参与者作为描述性补充样本。由于 E-DAIC 为 DAIC-WOZ 的扩展版本并包含原始 DAIC-WOZ 参与者，本研究未将两者合并，而是排除原始 DAIC-WOZ 参与者、仅保留新增有标签样本（候选 30 人，纳入 n=22，排除 8 人：transcript_empty）。该分析沿用 DAIC-WOZ 主分析中的十域症状定义、C5 抽取 prompt（v3）、模型参数（gpt-5.5 / temperature=0）与聚合规则生成症状证据特征。为规避接口对长输入的空响应，全文按不超过 3500 字符、相邻重叠 200 字符分块抽取，合并时按参与者、症状域、极性和原句做语义去重。模型在 DAIC-WOZ 主分析样本（n=142）上训练，并在 E-DAIC 新增样本上直接测试；E-DAIC AUC 的 95% CI 采用分层百分位 bootstrap（10000 次重采样，固定随机种子）估计。

> **偏差披露**：E-DAIC 转录本无 speaker 列（DAIC-WOZ 原始转录本含 Ellie/Participant 标签），故对 E-DAIC 喂入全对话文本；C5 抽取 prompt 要求仅提取被试症状证据，但由于 E-DAIC 输入为无 speaker 标记的全对话文本，仍可能存在访谈员话语干扰，因此该补充验证仅作探索性分析。

> **结构性非独立披露**：错位高低由 `coverage_breadth ≥5` 或 `evidence_density ≥11` 定义；base 模型使用这两项特征，count / presence 模型进一步使用构成它们的十域计数或出现特征。因而错位分组与模型预测并不独立，组间错分率及 Fisher p 值均不能用于验证「错位导致模型错误集中」这一机制。

## 2 结果（可粘贴进论文 3.4）

E-DAIC 新增有标签样本共纳入 n=22，其中 PHQ-8 阳性 n=6。采用 DAIC-WOZ 固定切点定义访谈证据高低（覆盖广度 ≥5 个症状域 / 证据密度 ≥11 条）。
- 覆盖广度定义下，错位样本占 9/22（40.9%，95% CI [20.7, 63.6]）；证据密度定义下占 9/22（40.9%，95% CI [20.7, 63.6]）。这仅表示固定错位定义在扩展样本中的描述性可迁移性。
- base 模型迁移 AUC 为 0.688（95% CI [0.375, 0.948]；n=22，阳性=6）。
- count 模型迁移 AUC 为 0.583（95% CI [0.292, 0.854]；n=22，阳性=6）。
- presence 模型迁移 AUC 为 0.583（95% CI [0.312, 0.844]；n=22，阳性=6）。
三个 AUC 的 95% CI 均跨过 0.5；这些点估计仅说明本样本中未出现明确的反向排序，不能证明模型泛化性能。

### 2.1 错分率输出的解释边界

补充 CSV 保留错位组与一致组的描述性错分率。例如，base 模型在覆盖广度定义下分别为 1.000 与 0.000。由于错位分组和模型输入共同使用症状证据变量，这一差异在结构上并非独立检验；即使 Fisher p 值很小，也不能排除定义—特征重叠造成的结果，因此不能视为对错位误差机制的独立验证。

## 3 讨论（可粘贴进论文讨论）

在 E-DAIC 新增可分析样本中，采用 DAIC-WOZ 预设切点后仍观察到一定比例的 PHQ-8 自评标签—访谈文本症状证据不一致。由于错位分组与迁移模型共同使用症状覆盖广度、证据密度及其域级组成特征，错位组中的较高错分率在一定程度上受定义—特征重叠影响，不能视为对错位误差机制的独立验证。迁移性能点估计及其宽置信区间仅作描述性报告。

因此，模块 14 只提供错位现象在 E-DAIC 新增小样本中的描述性复现，不验证模型泛化性能，也不验证错位与模型错误之间的关系。论文核心证据仍来自 DAIC-WOZ 主分析；本模块不进入标题或摘要核心结果，宜放在结果末尾一小段或补充材料。

## 4 对照表（摘要）

| 指标 | DAIC-WOZ 主分析 | E-DAIC 点估计 | E-DAIC 95% CI | E-DAIC n | 限制说明 |
|------|------------------:|----------------:|---------------|-----------:|----------|
| 样本量 | 142 | 22 | 不适用 | 22 | 仅含 E-DAIC 新增、有公开 PHQ-8 标签且转录本可分析者。 |
| PHQ 阳性比例 | 0.303 | 0.273 | [0.107, 0.502] | 22 (阳性=6) | PHQ-8 ≥10 为自评风险阈值，不等同于临床诊断。 |
| 覆盖广度错位比例(%) | 31.7 | 40.9 | [20.7, 63.6] | 22 | 固定预设切点的描述性可迁移性检查；不检验模型误差机制。 |
| 证据密度错位比例(%) | 19.0 | 40.9 | [20.7, 63.6] | 22 | 固定预设切点的描述性可迁移性检查；不检验模型误差机制。 |
| base 模型 AUC | 0.781 | 0.688 | [0.375, 0.948] | 22 (阳性=6, 阴性=16) | 95% CI 跨过 0.5；小样本点估计仅作描述，不能证明泛化性能。 |
| count 模型 AUC | 0.8 | 0.583 | [0.292, 0.854] | 22 (阳性=6, 阴性=16) | 95% CI 跨过 0.5；小样本点估计仅作描述，不能证明泛化性能。 |
| pres 模型 AUC | 0.796 | 0.583 | [0.312, 0.844] | 22 (阳性=6, 阴性=16) | 95% CI 跨过 0.5；小样本点估计仅作描述，不能证明泛化性能。 |
| base 模型错位错分率(覆盖广度) | 0.867 | 1.0 | [0.664, 1.000] | 9 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| base 模型一致错分率(覆盖广度) | 0.041 | 0.0 | [0.000, 0.247] | 13 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| count 模型错位错分率(覆盖广度) | 0.511 | 0.667 | [0.299, 0.925] | 9 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| count 模型一致错分率(覆盖广度) | 0.083 | 0.231 | [0.050, 0.538] | 13 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| pres 模型错位错分率(覆盖广度) | 0.578 | 0.889 | [0.518, 0.997] | 9 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| pres 模型一致错分率(覆盖广度) | 0.124 | 0.077 | [0.002, 0.360] | 13 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| base 模型错位错分率(证据密度) | 0.741 | 0.889 | [0.518, 0.997] | 9 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| base 模型一致错分率(证据密度) | 0.2 | 0.077 | [0.002, 0.360] | 13 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| count 模型错位错分率(证据密度) | 0.63 | 0.667 | [0.299, 0.925] | 9 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| count 模型一致错分率(证据密度) | 0.122 | 0.231 | [0.050, 0.538] | 13 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| pres 模型错位错分率(证据密度) | 0.667 | 0.889 | [0.518, 0.997] | 9 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |
| pres 模型一致错分率(证据密度) | 0.174 | 0.077 | [0.002, 0.360] | 13 | 定义—特征重叠：错位分组与模型输入共享症状证据变量；仅作描述，不能独立验证误差机制。 |

## 5 输出文件

- `edaic_extension_sample_manifest.csv`：30 候选→纳入清单与排除原因。
- `edaic_evidence_features.csv`：10 域 count/presence + coverage/evidence。
- `edaic_mismatch_quadrants_by_coverage.csv` / `_density.csv`：四象限。
- `edaic_transfer_predictions.csv` / `edaic_transfer_metrics_all_models.csv`：迁移预测、AUC 点估计与 bootstrap 95% CI。
- `edaic_model_error_by_mismatch_group.csv`：描述性错分率与精确二项区间；Fisher p 仅作为未经校正的补充输出，不用于机制验证。
- `daic_vs_edaic_validation_comparison.csv`：点估计、95% CI、样本量与限制并列报告，不含二元“方向一致”判断。
- `run_manifest.json` / `output_manifest_sha256.csv`：参数与可复现性清单。
