# Superseded source-importance outputs

`01_source_level_table/` through `06_manuscript_ready/` are preserved as historical audit artifacts only. They combine participant-aggregated base-model OOF probabilities with a second cross-validation layer and therefore do not meet the strict manuscript standard for fold-local raw-source modeling.

Do not use their tables, figures, AUC values, confidence intervals or significance claims in the paper. The replacement is `07_strict_joint_models/`.
