# Figure QA notes — strict source-importance manuscript

- Backend exclusivity: PASS; all four figures are generated by `reanalysis_v2/source_importance_figures.py` with matplotlib `Agg`.
- Export bundle: PASS; every figure has SVG, PDF, LZW-compressed 600 dpi TIFF and 240 dpi PNG.
- Editable text: PASS; SVG uses `svg.fonttype=none`; PDF uses TrueType font embedding (`pdf.fonttype=42`).
- Figure 1: PASS; workflow arrows and boxes are unclipped, and the inferential boundary is stated in the graphic.
- Figure 2: PASS; AUC estimates, seven ΔAUC intervals and a dedicated right-side `q` column are legible without overlap; all comparisons are marked `ns`.
- Figure 3: PASS; only domain counts carry `**`, matching `q = 0.0039`; the other two sources are marked `ns`.
- Figure 4: PASS; all 10 domains are visible in each panel, the 20-test family is stated and no domain receives a significance star.
- Visual encoding: PASS; no rainbow palette and no red/green-only distinction; point shape, position, text and intervals carry the result.
- Statistical unit: PASS; legends state participant-level repeated-OOF estimates, participant bootstrap, paired permutation tests and BH-FDR families.
- Manuscript integration: PASS; Figures 1–4 are inserted in methods/results immediately before full captions.
- Remaining journal-specific task: confirm the target journal's final dimensions, font minimum and TIFF color-mode requirements at submission time.
